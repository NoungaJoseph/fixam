$kind: http-request
name: Get Call History
order: 3000
method: GET
url: '{{baseUrl}}/api/calls/history'
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", () => pm.response.to.have.status(200));
